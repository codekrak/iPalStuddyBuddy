package android.companion;

import android.app.Service;
import android.content.Context;

/**
 * Stub for android.companion.CompanionDeviceManager (API 26+),
 * so tests compile under compileSdkVersion=25.
 */
public final class CompanionDeviceManager {
    private final Context mContext;
    public CompanionDeviceManager(Context context) {
        mContext = context;
    }
    // stub any methods your code invokes, e.g.:
    // public void associate(AssociationRequest request, Callback callback, Handler handler) { }
}
