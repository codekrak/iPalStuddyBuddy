package android.graphics.text;

/**
 * Stub for android.graphics.text.MeasuredText and its nested Builder
 * so tests can compile under compileSdkVersion=25.
 */
public final class MeasuredText {
    // no-op stub of the real class

    /** Stub the nested Builder so MeasuredText.Builder resolves. */
    public static final class Builder {
        public Builder() { /* no-op */ }
        // If your code ever calls methods on Builder, stub them here:
        // e.g. public MeasuredText build() { return null; }
    }
}
