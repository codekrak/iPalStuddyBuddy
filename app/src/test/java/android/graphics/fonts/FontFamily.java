package android.graphics.fonts;

/**
 * Stub for android.graphics.fonts.FontFamily (API 26+),
 * with nested Builder so tests compile under API 25.
 */
public final class FontFamily {
    // no-op

    /**
     * Stub of the nested Builder class.
     * Real signature takes a FontFamily.Builder constructor parameters.
     */
    public static final class Builder {
        public Builder() {
            // no-op
        }
        /** Stub build() so calling code compiles */
        public FontFamily build() {
            return new FontFamily();
        }
        // If your code calls other methods like addFont(...), stub them here:
        // public Builder addFont(...) { return this; }
    }
}
